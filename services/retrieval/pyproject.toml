[project]
name = "retrieval"
version = "0.1.0"
description = "RAG retrieval service"
requires-python = "~=3.12.0"
dependencies = [
    "jupyterlab>=4.6.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.22",
]
